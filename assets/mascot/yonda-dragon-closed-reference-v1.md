# Yonda 闭眼参考图

使用内置 image_gen 编辑原始小龙，得到 `yonda-dragon-closed-reference-v1.png`。此生成结果没有 alpha，不能作为全身透明帧直接替换。内置 UI 仅使用双眼皮肤内部的渐变遮罩区域；其余像素始终来自原始透明 PNG。ego-browser 已检查白色和深色背景，无整图背景切换。

## 生成提示词

Use case: precise-object-edit. 编辑目标：提供的小龙 PNG。用于同一个桌宠的眨眼动画闭眼帧。只把两只睁开的眼睛改成自然轻闭的眼睑，眼睑沿用奶白皮肤，闭眼线柔和。严格保持其余像素内容、头部与全身位置、比例、角度、表情中的嘴巴、翅膀、尾巴、爪子、光照和颜色不变，不能移动或重绘角色。保持同样的方形画布与完整角色布局。真正透明背景，保留 alpha，不加文字、不加场景、不加棋盘格。输出一张闭眼图。
