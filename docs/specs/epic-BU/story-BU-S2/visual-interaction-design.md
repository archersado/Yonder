# BU-S2 视觉交互设计

## 入口与流程

本增量无新按钮；任务面板后续可依据已提交引用显示“打开浏览器工作”。UI不能提交或修改external_task_ref。

## 状态与错误反馈

finished引用仅用于审计，不显示为可继续控制；引用缺失、Runtime不可用和结果unknown分别反馈，不能显示空任务或完成。

## 无障碍与平台验证

无新增视觉控件；结构化状态供后续键盘可达入口消费。macOS真实验证，Windows暂缓并明确不可用。

## 待决事项

DS任务卡片的外部入口随Gateway动作Story接线，本Story不提前增加不可用按钮。

## Browser引用只读详情（2026-09-18）

任务详情显示“未关联”、`ego:<编号> · Agent/用户控制 · N个托管页面 · 活动/已结束`或“关联状态读取失败”。只读摘要不显示URL、标题和正文。Agent控制的活动引用旁显示“打开 ego-lite”按钮；点击期间显示“正在交接…”，hand-off成功后收起Yonder任务面板。失败保留详情并给出明确反馈；已交给用户或已结束时不重复显示按钮。
