# DS-S1 视觉交互设计

## 四状态运行时接线（2026-09-14）

执行1.4秒四帧循环；等待用户500ms、暂停300ms入场后末帧保持，暂停保留慢呼吸。idle3.6秒呼吸帧，闭眼帧仅由独立3–5秒眨眼触发。减少动态效果直接展示静态姿态；拖动冻结，松开恢复当前真实状态。不可见停止序列，边缘双眼仍复用旧图眨眼。点击仅点头、hover任务菜单不变，窗口仍200×200，不移动整窗制造动作。

## 入口与流程

200×200；呼吸眨眼摇尾巴，探头双眼继续眨，点击恢复；完整双平台原生验证。

## 状态与错误反馈

宿主echo补验不新增UI：CLI仍报告固定版本echo，错误请求断开且不显示用户正文；Socket初始化失败阻止验证宿主假装完整启动，输出固定诊断；托盘正常退出同时关闭验证线程。无后台任务展示，不把echo线程当作执行任务。

呈现真实可观察状态；加载、等待、失败和未提供能力不能伪装为完成。无独立界面时由调用方界面或结构化响应承载，不额外建立窗口。

## 无障碍与平台验证

有 UI 的按钮提供名称与可见焦点，支持键盘，减少动态效果遵循系统偏好；无 UI 时保证调用错误稳定可区分。原生交互须各平台独立证据。

无障碍按钮必须接受系统语义激活（macOS AXPress）；复用点击唤醒/清醒反馈，不依赖鼠标位置。语义click与普通指针click区分，不能导致普通点击二次触发。2026-09-13原生AXPress返回成功但仍休眠的失败记录作为修复依据。

## 待决事项

补齐桌面基础栈与跨桌面证据；正式任务忙碌事件尚未接线。

2026-09-12 全屏验证增量：依 AD-E0-01，在 macOS 候选窗口初始化补全屏辅助行为；普通桌面/全屏切换应保持同一小龙与坐标。真实纯色全屏窗口的前后区域截图作为验证，不以可见性布尔值替代。Windows 不受此条件编译改动影响，但未复测不得声明通过。

最终轻量形态：macOS Accessory + LSUIElement，保留桌宠/托盘，去除普通 Dock 入口；禁止全屏平铺；macOS 13+ CanJoinAllApplications 允许小龙作为其他全屏应用的悬浮辅助窗口，不抢焦点。清醒形态原生验证见 verification-fullscreen-overlay-macos.md，其他门禁未关闭。

2026-09-13 内存优化实验：用户授权保留原图并生成运行时副本；主体600×600、探头336×168适配当前尺寸3倍缩放。动画时序、掩膜、交互和原始画稿保持原有设计。只替换图片引用，以截图及相同五分钟展开资源采样验收；画质或内存不合格返回修复。

## 有任务才显示与忙碌形象（2026-09-14用户变更）

用户再次指出悬停未显示，并明确要求无任务不弹面板、有任务运行时显示忙碌动画。此处“有任务”定义为存在未结束任务（created/running/waiting-for-user/paused/interrupted）；已完成/失败/取消仍可从托盘历史查看，不触发自动悬停。来源为本次用户变更，映射DS-S2 AC2/AC5/AC6与DS-S1忙碌不隐藏要求。

验收：空库悬停不显示；有未结束任务悬停显示；同一悬停期间任务从无到有也显示；running或准入占用进入忙碌形象，结束后恢复闲置；等待/暂停仍能打开面板但不伪装正在执行；读取失败标记未知，不播放执行动画或作为收起许可。运行任务开始后已收起小龙唤醒；拖动不误开。减少动态效果只保留静态忙碌提示。

架构：TaskHost从既有TaskStore读取未结束列表limit1，复用activity()读取运行/准入事实；Rust返回两个派生布尔值，不新增持久化/任务状态所有者。专用本地pet命令固定窗口身份，在blocking worker查询SQLite；前端只保留瞬时表现。忙碌动画复用既有呼吸/尾巴/响应变换，加明显的“忙碌”提示，不生成新素材。原生悬停维持窗口内同坐标系检测，查询周期不作为严格性能门槛。

## 同桌面与面板操作（2026-09-14用户补充）

面板与小龙必须在同一桌面/Space，同进程应用相同原生窗口集合行为；不切换到其他Space打开。鼠标从小龙跨间隙进入面板后保持，面板上的现有任务按钮可操作；离开面板与小龙区域后隐藏，跨间隙留350毫秒宽限，已经进入面板后离开立即在下次原生观察隐藏。当前只读任务按钮仍为选择详情、刷新、分页，暂停/取消未接通不得假装执行。

小龙任务表现复用assets/mascot/互动动画生命周期-v1.md：真实执行占用映射executing（认真挥爪，约1.4秒循环），等待用户映射waiting_for_user，暂停/中断映射paused，其余idle。执行优先，非执行时首个未结束任务作为代表（按任务ID）；成功/失败一次性动画仍需真实事件去重，当前快照不重播。executing使用同角色透明四帧素材，不把整张PNG变换伪装成局部挥爪。此补充覆盖此前新增文字忙碌标记的工程建议。

透明帧验证失败后改用已有透明PNG局部爪层，按生命周期文档补充设计实现executing微挥爪；完整waiting_for_user举爪、paused收翼素材尚未完成，状态切换先接通，不能宣称这些姿态已验收。

## 生命周期素材子范围设计（2026-09-14）

来源为产品简报桌宠、架构九种展示状态、既有互动动画生命周期-v1.md和用户“先把素材都生成”/执行形象不对反馈。子范围设计审阅通过，只生成同一角色九种状态（按用户最新指定，仅本批九状态），每项4帧2×2真实透明图集，原图保留。ASSET-01角色一致；02状态姿态语义可辨；03真实alpha/完整边缘/安全锚点；04清单与提示词对应，退出无延迟、Recording红点保持独立。仅素材子范围设计ready，图片须经过独立验证，不开放任务虚拟状态，不替代正式UI/原生动画验证，运行时接线门禁保持。

2026-09-14素材处理授权：用户明确允许Python/Pillow去背景、拆帧和统一画布并保留原图。输出400×400 RGBA序列帧，基准200×200逻辑像素；共同缩放和脚底锚点，成功跳跃保留最多8逻辑像素偏移。中性棋盘背景按颜色与连通区域提取，不能侵蚀角色高光；处理结果须人工检查深浅背景，失败帧不接入运行时。

2026-09-14用户回归：背景格子残留、四帧硬切不丝滑，RUN-ASSET整体失败返回Apply。重新生成纯绿色分离底并提取真实alpha。播放改为两帧连续平滑混合，复用可见时钟写透明度，避免后台WKWebView CSS时间线冻结；入场保持仍按原时长，减少动态效果/未知/拖动保持静态。待命使用固定姿态加连续呼吸，不以每900ms换身形代替呼吸；眨眼为短暂闭眼帧过渡。帧间混合可能产生重影，必须目测，不以索引/透明度变化证明丝滑。

v4整图执行循环混合出现爪部重影，目测拒绝。执行改用新完整专注姿态第0帧作稳定身体，从同帧按爪根局部分层连续交替旋转（1.4秒），不使用整图交叉淡化伪装挥爪；等待/暂停只保留短过渡。该修复保留新形象与透明度，不退回旧待命形象。待命连续呼吸与局部尾摆，闭眼帧短过渡。

## 全状态基础动画与短臂回归修订

2026-09-14用户反馈与修订：全部九态保留呼吸/眨眼，执行双爪主要上下动作、肩臂带动，不能只旋转掌部贴图。180ms眨眼含闭合、短保持、睁开，只改变眼区；基础动作不覆盖求助/暂停姿态或录制指示。九态QA页面明确四态当前播放器、五态未接线姿态样本，支持入场重放/深浅背景；真实桌面接管/Recording不因预览开始。验收BASE-01/02、ARM-01分别需动态采样与目测，Windows继续暂缓。


## 2026-09-14 用户变更：九态专属循环

来源：用户“呼吸眨眼所有的状态都要有”“每个状态都要有自己的动作动画重复播放”。覆盖此前完成/失败单次、等待/暂停仅保持的视觉播放规则。状态退出立即打断，不重复发布事件或自动恢复任务。九态独立呼吸、3～5秒眨眼，并按状态循环：idle呼吸摇尾；listening抬身点头；thinking交替歪头、尾尖轻摆；executing短臂围绕肩部交替抬落；waiting_for_user举爪姿态轻点头；paused收翼姿态慢下沉放松回位；recording专注姿态慢摇尾；success轻跳落稳；failed低头后回看用户。暂停首版不反复展开翅膀。减少动态效果、拖动、隐藏停止装饰动作。

采用现有透明关键姿态与连续变换，不用全身交叉换帧模拟挥爪；不新增依赖、状态所有者或任务协议。四个真实来源状态沿用产品播放器，其余五态仅在独立预览模拟。验收逐态观察至少两轮动作、呼吸、完整闭眼再睁眼，执行爪根不断裂、背景无格子。浏览器证据不替代原生验收，Windows继续暂缓。


### 九态循环返工：专属动作必须可辨认

用户反馈除待命/执行外看不到动作，上一版仅整体轻晃不足以通过。撤回“九态动作通过”的产品结论。收到请求/等待响应改为头部独立抬动与左右歪头，身体固定；等待用户使用放爪到举爪的循环；暂停使用展开到收翼的循环；完成使用展翼举爪小跳；失败使用低头到回望；录制加强独立尾部慢摆。等待/暂停/完成/失败复用既有四帧关键姿态，来回播放并短暂停顿，避免直接从末帧跳回首帧。执行维持连续肩部抬爪，不恢复全身换帧。所有状态继续呼吸与独立眨眼。验收增加头部局部矩阵及多张姿态索引变化，不能只判断任意 transform 变化。局部分层原图保留，透明边缘与头颈接缝需目测验证。

失败状态帧混合在截图中出现重影，返回实施：改为单一回望姿态的头部局部分层，连续下移/俯转再回位；闭眼层使用相同矩阵，不混合两个完整头部。

用户追加“需要流畅的动画”：停止整图四帧混合用于状态动作。等待用户与成功提取单一举爪姿态的前臂，躯干仅在遮罩内补齐，前臂围绕肩部连续旋转；暂停保持收翼躯干，仅独立翼片围绕翼根连续俯转开合；成功跳跃连续缓冲。所有循环使用同一时钟与平滑正弦曲线，不重播事件，拖动/减少动态效果停止。验收要求躯干无整图交叉混合，动作局部矩阵连续变化，视觉接缝另行检查。


### 录制道具与动作语义（用户验收目标）

来源：用户要求准确表达状态，举例“录制这种可以拿出相机拍照闪光之类的，可以用道具来丰富动作”。录制改为双爪抱相机：低位准备、平滑举起、短暂停留拍照、局部低强度闪光、平滑放下，每轮4.8秒。相机与前爪共享运动锚点；红色录制指示稳定显示。闪光仅在镜头附近、每轮一次，不覆盖屏幕，不影响点击/拖动；减少动态效果保留静态相机与红点，不闪光。相机为独立透明位图，保留生成原图。

当前录制真实来源尚未接线，只在独立预览模拟；不得启动真实录制或调用拍照能力。验收需看到举起/放下、镜头附近闪光、连续呼吸眨眼，记录长周期和透明背景证据；用户满意尚未证明，不可用transform变化宣称完整目标通过。

等待状态返工：原生预览截图可见等待外部响应头颈接缝，等待用户持续摆爪像招手。头颈桥接区补齐并减小歪头角度；等待用户改成举爪停留后轻提示。分别使用小型省略点气泡（外部响应）与问号气泡（用户处理）明确语义，不表示Yonda在规划；保持循环、呼吸眨眼。提示不获取输入，不触发任务。用户具体问题尚待回复，此处先修已观察到的缺陷。

用户明确缺陷：等待用户前臂出现裁切痕迹、等待外部响应头部鳞片脱离。修复验收要求：头部遮罩包含完整侧鳞片；前臂遮罩完整覆盖前臂并柔化肩部接线，动作极值截图无切口、鳞片与头部同步。不能用提示气泡代替分层修复。

浅色背景极值检查发现尾部1像素分层线：所有状态现为单关键姿态连续动作，主图恢复普通alpha混合；尾片在零位移轴处增加1像素覆盖，避免两次裁切抗锯齿留下空隙。不恢复整图交叉混合。原图与动作语义不变。

减少动态效果验证修复：暂停态隐藏独立展开翼片，保留paused-03收翼主体；相机保留静态低位道具，闪光为0。ego浏览器仿真必须显式传media空字符串，否则不构成系统偏好证据。

状态辨识补充：暂停的收翼静态姿态仍可能被理解为普通站立，增加小型“Ⅱ”状态标记；收到请求增加小型“✓”确认标记；等待外部响应的三个省略点按顺序连续明暗流动。标记不承载任务详情、不接受点击；减少动态效果时保持静态，关闭明暗循环。


### 2026-09-15 撤销易开裂的局部分层

用户实机反馈：收到请求、失败状态头部裂开，等待用户手边有圆圈且需明确抬手，成功形象被切割。上述状态局部分层验收失败，撤销相关头/爪切片和圆形提示。收到请求、等待外部响应、等待用户、成功、失败均使用一张完整RGBA角色姿态作为主体，以连续整体位移/转动表达动作，杜绝头颈/手臂切口。等待用户固定使用完整举爪姿态，在该姿态上做轻柔抬身循环；不再显示问号圆圈。成功使用完整举爪展翼姿态做余弦小跳；失败使用完整低落姿态做缓慢下沉回位；收到请求使用完整抬头姿态做点头确认。保留呼吸和独立眨眼。旧局部素材保留为失败证据，不再由这些状态引用。

等待用户修正：用户所称“手上有个圈”对应问号圆形提示，已移除。等待用户仍使用经前一轮扩大并柔化的完整前臂层，围绕肩部连续抬起；主体为同姿态补齐躯干。收到请求、等待外部响应、成功、失败不再使用头部或庆祝爪局部分层。

用户纠正：上一轮只需移除等待用户的问号圆圈，不应删除其他有辨识作用的图标。恢复收到请求✓、等待外部响应流动省略点、暂停Ⅱ；等待用户保持无圆圈并使用抬爪动画。图标恢复不恢复任何头部或成功态切片。

2026-09-15 视觉验收：等待用户从双爪收于胸前开始，右爪经两个过渡姿态抬至脸侧，短暂停留后原路放回；掌面不显示粉色圆形。录制相机固定在胸前双爪之间，红色 `● REC` 全程可读；按快门时相机仅下压约 1px，闪光局限在镜头附近但亮度足以在深浅背景辨认。不得牵动肩膀或遮挡眼睛。

### 收到请求真实接线

成功接受 Agent 创建请求后，小龙使用完整抬头姿态轻点头，右侧 ✓ 由 82% 缓动到 100%，持续约 1.6 秒；同时保留呼吸与一次可发生的独立眨眼。若正在执行则继续忙碌动作。休眠态先恢复到原位置再播放；拒绝请求不闪现 ✓。减少动态效果保留静态抬头与 ✓，脉冲到期仍按真实状态退出。

隐藏态发生展示状态变化时，双眼立即执行既有恢复过渡，完整小龙出现后直接呈现新状态。等待用户和异常状态优先让用户看见；同一状态重复刷新不抖动、不重播唤醒。状态在恢复动画期间再次变化时只更新最终姿态，不启动第二次恢复。

EXEC-PRESENT-01：CUA/BUA调用开始事件必须立即切入现有`executing`挥爪、呼吸与眨眼循环并唤醒；即使任务查询锁被执行占用也不得停在`listening`或`idle`。调用退出事件按真实派生结果切换，不额外播放成功或失败；前端不以定时数据库查询驱动状态。

### 终态一次性展示增量（2026-09-18）

真实完成进入 `success` 现有完整角色轻跳循环，约 1.8 秒后回到同次提交后的派生状态；真实失败进入 `failed` 现有低落回望循环并按相同时长退出。终态到达时若 Yonda 趴在边缘，先调用既有唤醒过渡，状态立即记录，完整形象恢复后继续显示剩余终态反馈。相同 `task_id/sequence/status` 再到达不重播。减少动态效果显示对应静态姿态但仍遵守退出时间。终态动画不打开任务菜单、不改变任务数量，也不把读取历史或错误响应表现为新结果。
