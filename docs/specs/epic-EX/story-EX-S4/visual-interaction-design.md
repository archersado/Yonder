# EX-S4 视觉交互设计

任务卡继续展示实际执行/等待/失败；文档结果以现有产物入口显示另存路径和可检查结果，不展示内部 OOXML 或模型候选。覆盖文件、Shell、提权、安装、删除、支付、发送沿用用户显式确认界面；拒绝后冻结候选并反馈给 Agent。缺参、文件锁、哈希冲突、命令失败及 `unknown` 分别显示可读原因，不能统一显示「成功」或默默重试。按钮可键盘操作，有可读状态；macOS/Windows 各验证真实权限与失败反馈，不保存敏感正文/完整输出在截图或日志。
