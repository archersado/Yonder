# RC-S1 产品需求

## 问题与目标

仅用户手动开始和停止；可靠识别user来源并形成不可变原始时间线，为RC-S2派生Trajectory提供可信输入。

## 范围与非目标

本Story负责手动录制、来源分类、隐私排除、完整性缺口和不可变原始时间线。Trajectory派生、步骤审阅、版本库和回放归RC-S2；不得默认开启录制。

## 验收条件

- 目标行为有可复现成功样本，失败不得伪报成功。
- 仅用户手动开始和停止；只把可靠的user输入写入原始时间线，其他来源不得成为RC-S2的可回放输入。
- 平台限制与未实现项明确；涉及 UI/Driver/权限时分别提交 Windows/macOS 原生证据。

## 待决事项

输入正文的未加密持久化仍待后续ADR；Trajectory动作格式、审阅确认与Replay由RC-S2承接。首个Spike只保存无正文分类证据。

## 需求来源

- 产品依据：[产品简报](../../../../_bmad-output/planning-artifacts/briefs/brief-Yonder-2026-09-09/brief.md)、[补充材料](../../../../_bmad-output/planning-artifacts/briefs/brief-Yonder-2026-09-09/addendum.md)，对应章节：显式上下文录制、MVP 主干链路、MVP 明确不做。
- 架构约束：[架构主干](../../../../_bmad-output/planning-artifacts/architecture/architecture-Yonder-2026-09-09/ARCHITECTURE-SPINE.md)，对应章节：Recording 与桌宠、个人上下文。具体 ADR 以架构设计列出的状态和平台范围为准。
- 本 Story 是上述需求的模块内分解，不代表整条产品链路完成；跨模块与遗漏项见 [覆盖映射](../../REQUIREMENTS-TRACEABILITY.md)。具体阈值、字段和布局若无原文依据，应作为设计建议审阅，不能称为用户要求。

## 人工接管记录与交回（2026-09-14用户变更）

来源：本次用户明确执行中支持人工接管并记录用户行为，作为交回Agent的Observe依据；架构Recording与桌宠/任务恢复约束，Accepted AD-TM-03。仅Agent创建任务，人工接管不创建新任务。

验收映射：TAKE-01显式接管阻止Agent新动作，停止未确认不称已移交；TAKE-02本次手动接管开启可见、可停止的记录，只录user输入，密码/安全界面/排除应用不采；TAKE-03原始时间线不可变并关联原task_id；TAKE-04交回保存证据并执行新鲜Observe，向归属Agent交付轨迹/证据引用及当前状态；TAKE-05交接失败/配额缺口明确反馈、保持暂停，不自动续跑；TAKE-06Agent重新Observe并显式恢复，重新准入后才executing。

TM-S3管接管及停止确认，RC-S1管用户记录/不可变证据，TM-S5管任务时间线引用，TM-S4管交回及显式恢复，AG管归属Agent交接协议，DS-S2展示已有任务接管/记录中/交回。自动输入干预只暂停，不无提示开启Recording；显式接管作为用户手动开始。已有每设备Recording时明确冲突，不覆盖。

UI流程：任务面板“接管”→“正在停止Agent控制/记录中”→停止确认后人工控制；小龙按既有暂停表现并保留独立录制提示。用户可以停止记录，任务仍保持人工接管；交回时如果停止后有未记录操作标注证据缺口，最终Observe仍必须新采集。“交回Agent”不等于立即继续，不用点击桌宠自动恢复任务。

协议/持久化/Driver停止与授权/隐私字段未定稿，相关Story保持设计阶段，不提前实现采集或向外发送用户记录。Windows暂缓、真实双平台证据保留。

## 需求分类与首个增量（2026-09-17）

- 原始需求：默认不采集；用户主动开始Record；记录授权范围内的操作时间线与上下文；桌宠呈现recording；原始素材30天、轨迹长期保留。
- 后续用户变更：任务人工接管时记录用户行为，作为交回Agent的Observe依据。
- 架构约束：每设备单录制、原始时间线不可变、仅`user`派生轨迹、密码/安全界面/排除应用不采集、日志无正文、交回不自动恢复。
- 待审设计建议：macOS优先验证CGEventTap listen-only与AX隐私门禁；队列溢出/Tap失效产生显式gap；Spike不保存正文。

2026-09-18架构修订：因 macOS 无法稳定证明逐条事件的物理用户来源，首轮只记录显式接管租约内的`controlled_session_input`，作为交回前 Observe 证据；它不等同物理用户输入，也不能作为自动回放输入。

首个增量验收：REC-01默认关闭及显式边界；REC-02单设备冲突；REC-03来源拒绝；REC-04隐私排除；REC-05停止与缺口；REC-06桌宠/菜单只展示事实。Replay和交回对应REC-07，待Spike与后续联合设计。
