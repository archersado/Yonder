# 设计

统一样本直接以程序路径和参数启动；Shell只作为生成子进程树的测试夹具，不是候选产品接口。macOS使用独立进程组并在超时后终止整组；输出样本只保留64 KiB前缀并标记截断。Windows样本后续使用Job Object复现同一断言。

通过条件：参数保持字面值；输出不超过预算；父进程及子进程均退出；结果只含退出/超时/截断分类，不记录正文。任一平台失败则AD保持Proposed且产品实施不得开始。
