# CU-S2 原生 WorkRef 定位

关联 Story CU-S2 与 Accepted AD-CU-03。Architecture Impact：architecture-change（Application Port与macOS原生Adapter）。在有效Observe后捕获绑定完整执行身份的宿主内WorkRef，定位前重新核对进程启动身份与WindowServer/AX双侧唯一映射。无外部协议、SQLite、UI或Recording变更。
