# CX-S2 macOS Preview 验证目标

状态：实施自测通过，等待独立验证
范围：仅 macOS 当前显示器的圈选提问 Preview。

## 复核结论

Story 与 OpenSpec 已限定 macOS 单显示器 Preview，且未新增依赖、协议或持久化。Application 现在持有临时会话与截图生命周期，关闭和重新圈选会清零截图；确认卡明确换算为 440×560 点。2026-09-20 实施自测已验证框选、笔画、确认卡和取消清场，见下方证据。Esc、超时、权限拒绝、CUA租约拒绝和数据清理矩阵仍须由独立验证者复跑，当前不得 Archive。

## 需求摘要与 Preview 验收映射

用户通过小龙或菜单栏显式启动，在当前显示器框选或画圈；有效区域生成只驻内存的截图并显示无蒙层确认卡。Preview 只允许重新圈选或取消，发送能力明确待接入。

| AC | 本 Preview 可验证部分 | 不在本 Preview |
|---|---|---|
| CX2-01 | 两个显式入口；空闲时无可见覆盖层、截图或常驻指针监听 | 快捷键注册与设置 |
| CX2-02 | 当前显示器短生命选择层；完成、Esc、取消、30秒超时均恢复指针并清场 | 切屏、显示器变化与进程崩溃恢复 |
| CX2-04 | 完整截图缩略图、重新圈选、取消及“发送待接入”文案 | 问题输入、语音、发送及真实提交结果 |
| CX2-05 | 截图、选区与笔画只驻有界内存；不写日志、任务事件、Outbox、索引或文件 | Agent临时附件生命周期 |
| CX2-06 | 未授权时稳定拒绝，不伪装成功，且不保留截图 | 仅文字/语音降级与系统设置入口 |
| CX2-07 | 活跃CUA租约时稳定拒绝且不显示覆盖层、不争夺指针 | 暂停任务及后续恢复语义 |

明确非目标：Agent输入发送、任务创建、问题输入、语音组合、多显示器、负坐标、运行中撤权、Windows、快捷键、常驻监听和持久化。

## 前置条件与证据规则

- 使用 CX-S2 Story worktree 构建的同一提交；记录提交 SHA、应用 bundle ID、macOS 版本、显示器数量/缩放和二进制 SHA-256。
- 成功路径只圈选无敏感内容的本地四色测试窗口。验证记录只能保存布尔值、时间、窗口/选区尺寸、错误分类、计数和哈希；不得保存截图、base64、问题正文、窗口树或完整日志。
- 权限成功与拒绝分开运行：成功路径使用已授权的正式 Preview；拒绝路径使用新的临时 bundle 身份，仅预检并运行拒绝路径，不重置或覆盖现有 Yonda 权限。
- 运行前后只读记录任务、事件、Outbox和索引的行数，以及应用数据目录和受控临时目录的文件名/大小/mtime清单；不得读取或复制正文。

## 最小实机验证矩阵

| 场景 | 操作 | 无敏感正文证据 | 通过判据 |
|---|---|---|---|
| 入口与空闲 | 分别从小龙和菜单栏打开、取消；再空闲观察 | AX窗口存在/消失布尔值、入口名称、进程树与窗口计数 | 两入口均只产生一个选择层；取消后窗口不可见；无子进程、常驻指针进程或空闲截图活动 |
| 成功圈选与确认卡 | 在四色测试窗口各完成一次框选和画圈 | 选择/确认窗口尺寸、模式、确认卡控件存在、捕获返回字节数与PNG尺寸；不记录字节 | 选择层退出；确认卡完整显示缩略图、重新圈选、取消和待接入文案；笔画按外接矩形捕获 |
| Esc | 选择中按物理Esc；确认卡再按物理Esc | AX窗口消失、指针位置恢复、Application会话`idle`、截图字节数为0 | 两个阶段均在3秒内清场，鼠标可正常操作，再打开无旧预览 |
| 取消与超时 | 分别点击工具条取消、确认卡取消；选择中和确认卡各等待30秒 | 每条路径的结束原因、窗口消失时间、会话状态和截图字节数 | 四条路径均清场；超时不早于30秒且在32秒内完成；再打开无旧预览 |
| 权限拒绝 | 用全新临时bundle身份启动并圈选测试窗口 | `permission-required`、`permission_requested=false`、窗口清场、截图字节数0 | 不出现缩略图、不写文件、不弹伪成功；正式Yonda权限不被重置或修改 |
| CUA租约拒绝 | 用现有测试夹具建立真实Desktop租约，两个入口各启动一次，随后按既有夹具清理任务 | `desktop-control-active`、租约持有任务ID哈希、覆盖层未出现、CUA动作计数不变 | 两入口均拒绝；不抢焦点/指针，不新增动作，不改变任务状态或sequence |
| 临时数据清理与边界 | 完成成功、Esc、取消、超时、权限拒绝和租约拒绝后比较前后快照 | 会话`idle`、截图/笔画/选区字节数0；任务/事件/Outbox/索引计数和文件清单差异摘要 | 无截图文件、base64日志、任务/事件/Outbox/索引变化；仅允许验证证据JSON本身新增 |

任一行失败即返回实施阶段；不得以截图、人工口述或静态代码检查代替结构化判据。所有行通过后，独立验证者把本文件状态改为“通过”，附证据目录和命令，再允许 Archive。

## 已有检查

- 2026-09-20：正式 macOS Preview 真实运行，框选、笔画、确认卡 440×560 点、取消清场、选择层/确认卡 Esc 清场，以及选择层 30.264 秒超时清场均通过；用户随后手工复核确认卡超时通过。见 `apps/desktop/evidence/cx-s2-region-preview-macos-20260920/result.json`。证据不含截图正文。
- `cargo check -p yonder-desktop` 通过。
- `python3 scripts/check_architecture.py` 与 `git diff --check` 通过。
- `cargo test -p yonder-application admission::tests --lib` 通过。

这些结果只证明构建、架构静态规则和通用Admission测试，不证明上述真实Preview交互。

## 风险、阻塞与延期

- 阻塞：Application会话所有权、结束时截图字节清零和无正文诊断尚未实现；修复前不可调度独立验证。
- 风险：macOS TCC按bundle身份授权；拒绝测试若复用正式身份会污染用户权限。必须使用临时身份，禁止自动执行`tccutil reset`。
- 风险：入口错误当前对用户可能静默。研发须让小龙和菜单栏都显示同一稳定错误分类，证据不得只依赖stderr。
- 延期：Agent发送、文字/语音降级、暂停CUA、多显示器/负坐标、显示器变化、运行中撤权、进程崩溃恢复、快捷键和Windows；这些不影响本Preview验证，也不得标记CX-S2完整Story完成。
- 无需用户决策：以上均由已接受Preview边界决定。只有需要重置正式Yonda屏幕权限时才必须先取得用户明确同意；当前方案不需要该操作。
