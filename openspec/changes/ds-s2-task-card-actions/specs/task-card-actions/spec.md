# 任务卡片操作

## ADDED Requirements

### Requirement: 直接操作
每卡片提供接管与取消任务，详情为兄弟按钮不嵌套。created取消可用，其他状态禁用说明；请求中禁用、错误可见，全部保留历史。不提供同义删除/清理按钮。

### Requirement: 接管门禁
执行中才适用接管，停止确认与Recording未接通禁用并说明，不伪报已接管。键盘焦点支持，不触发其他任务或详情。
