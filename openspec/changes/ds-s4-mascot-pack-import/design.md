# 设计

资源包仅含 `manifest.json` 与 `assets/` 下的一级 PNG/WebP。manifest 为 `format_version: 1`，包含九个固定状态；每状态 1～16 个唯一帧。ZIP 源、解压总量、单帧、像素和条目限额固定为：64 MB、32 MB、8 MB、1024×1024、64 MP、145。宿主在暂存目录完成路径、类型、大小、像素、状态集与帧校验；仅全部通过后用备份重命名和带回滚的目录切换激活包。WebView 只加载宿主提供的已验证资源描述；失败回退旧包，不执行包内代码。
