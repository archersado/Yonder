# DS-S1 清醒形态普通桌面往返独立 Verification Goal

Story：DS-S1；OpenSpec：e0-validate-desktop-foundation；日期：2026-09-13。

依据：增量规格「切换桌面空间」。状态：未通过，本轮未取得合格清醒A→B→A样本；不扩大既有右侧休眠往返证据。

## 工具范围

基于历史诊断脚本提取check-awake-spaces-macos.swift，去除其仅适用于上次恢复的“既有桌面2视为临时桌面”逻辑。只在初始一个普通桌面时创建一个临时桌面；从原生列表确认1→2后记录尾部新增桌面，结束时只删除本轮记录项，恢复原窗口。已有多个普通桌面时不创建/删除，不猜测归属。

不使用私有Space API。以Dock的Mission Control辅助功能列表选择桌面，捕获小龙对应屏幕区域。最终工具增加清醒200至201尺寸、主屏坐标范围与截图退出码检查；在关闭Mission Control后才调用既有托盘探针唤醒。无产品实现改动。

## 实际运行

证据目录均位于spikes/desktop-foundation/evidence/：

| 目录 | 结果及清理 |
|---|---|
| awake-spaces-20260913 | A采样已经是56×112休眠形态；B的x=-1356在屏幕外，区域截图失败；返回切换失败。AXRemoveDesktop=0，普通桌面恢复为1，原窗口AXRaise=0。不能记作清醒验收通过。 |
| awake-spaces-20260913-retry | 新增临时桌面后调用唤醒探针读到0×0尺寸，探针失败，测试停止。AXRemoveDesktop=0，桌面恢复为1，原窗口AXRaise=0。未取得任何合格跨桌面采样。 |
| awake-spaces-20260913-settled | 调整为先关闭Mission Control后唤醒，但入口阶段无法取得Mission Control列表，退出1；该轮尚未创建临时桌面。 |

前两轮JSONL完整记录清理。第三轮在入口失败，只有工具控制台错误“Mission Control不可用”，不虚构后续截图或恢复日志。已将原窗口恢复defer移至更外层，覆盖最终工具的入口错误；该调整重新编译成功，尚未重新执行原生入口失败路径，不宣称其运行通过。

本轮不能证明问题来自产品跨桌面实现，已观察到测试前置状态/坐标过渡及系统自动化入口问题。停止连续创建桌面重试，保留失败记录。没有变更系统桌面布局配置或产品窗口策略。

## 后续条件

需要可稳定操作Mission Control的原生测试环境，或在用户实际切屏时直接记录同一清醒窗口的屏幕区域；仍须清醒尺寸、同一编号、坐标一致及实际形象可见四项同时成立。用户之前确认点击眼睛唤醒，不可替代本项。

Windows、拖动及菜单仍遵守各自暂停要求。DS-S1总Goal未通过。
