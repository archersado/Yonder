# DS-S1 普通桌面往返独立验证

Story: DS-S1
OpenSpec: e0-validate-desktop-foundation
日期：2026-09-12
依据：AD-E0-01，桌面规格“普通 Space 切换保持同一窗口与原位置”。
状态：本机右侧休眠形态通过；Story 不 Archive。

## 范围与环境

沿用最终轻量插件构建 PID 66418，无产品代码变更或重新构建。之前快捷键切换未取得成功通知；本轮检查系统公开 Mission Control 辅助功能界面，确认实际只有一个普通桌面，其余是全屏应用空间。因此临时创建第二普通桌面，使用系统 AXPress 选择 A→B→A，再移除本轮新增桌面。

首次新增后系统自动把原“桌面”重命名为“桌面1”，旧标题比较导致检查停止；再次读取原生列表确认新增为“桌面2”后才执行续作。没有猜测删除其他桌面。

## 结果与证据

目录 `spikes/desktop-foundation/evidence/normal-spaces-20260912/` 保存原始 JSONL、三张区域截图及本轮专用恢复脚本。脚本含固定临时名称，只作为证据，不是可泛用的清理工具。

- 确认两个条目均为普通桌面，不把全屏应用计入。
- A、B、返回 A：同一窗口编号 20605，原生边界均为 (1654,758,56,112)，三次截屏均成功。
- 三张屏幕区域人工核对均有右侧双眼入口。B 的桌面背景不同，未以单窗口缓存或 onscreen 标记代替实际屏幕内容。
- 临时“桌面2” AXRemoveDesktop 返回 0；随后普通桌面数量为 1，恢复原应用窗口的 AXRaise 返回 0。
- 本次没有测量完整三分钟休眠时长、眨眼频率或清醒形态普通桌面切换；不能扩展通过范围。

## 剩余门禁

清醒形态普通桌面切换、其他边缘/多显示器、Windows 本轮验证、全进程资源预算和宿主 IPC/权限拒绝等仍需验证。当前完成的是一个真实原生场景，DS-S1 与 AD-E0-01 保持未完成。
