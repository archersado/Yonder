# DS-S1 休眠全屏与返回唤醒独立验证

Story: DS-S1
OpenSpec: e0-validate-desktop-foundation
日期：2026-09-12
依据：AD-E0-01 全屏辅助显示补充。状态：本机右侧休眠与返回唤醒通过，Story 不 Archive。

## 环境与范围

沿用上一轮最终构建 PID 66418，Accessory 模式；本轮未修改或重编译产品代码。临时纯色 AppKit 测试窗口进入全屏并自动退出，随后经 Yonda 原生托盘菜单唤醒。Yonda 自身始终是轻量桌宠。

## 证据与结果

目录 `spikes/desktop-foundation/evidence/rest-fullscreen-overlay-20260912/` 保存诊断脚本、fullscreen.jsonl 和各阶段屏幕区域截图。

- 开始时小龙已自然休眠，原生窗口编号 20605，边界 (1654,758,56,112)。未修改产品的三分钟计时，也未测量这次休眠的完整起始时间。
- 其他测试应用全屏后 1 秒、4 秒及退出后，小龙编号、位置和大小完全一致；测试应用保持激活，全屏 4 秒屏幕区域人工核对能看到右侧双眼入口。
- 返回后原生“显示小龙” AXPress=0；1 秒和3秒后恢复 (1468,713,200,201) 的原生边界，截图可见完整小龙，无裁切。201 为 CG 测得边界，产品配置仍是200×200逻辑尺寸。
- 临时测试程序正常退出，Yonda 保持运行；本次不验证原生鼠标点击眼睛、全屏内眨眼频率或其他边缘。

## 普通桌面切换单独记录

`evidence/space-roundtrip-20260912/` 尝试 Control+Left，起始前台窗口 AXFullScreen=true；2 秒内未收到 activeSpaceDidChange 通知，脚本退出2且未发送反向快捷键。不能确认切屏成功，更不能认定“普通桌面之间往返”通过。此次起始场景本来就是全屏，故也不满足普通桌面验收前置条件。保留原始记录，不扩大通过范围。

## 剩余门禁

普通桌面切换、Windows 本轮验证、全进程资源预算、宿主 IPC/权限拒绝等仍待完成。不能依据本次局部原生验证将 DS-S1 或 AD-E0-01 标为完成。

后续已通过 Mission Control 的两个普通桌面真实往返补齐右侧休眠场景，见 verification-normal-spaces-macos.md。此前快捷键尝试仍保留为未通过记录。
