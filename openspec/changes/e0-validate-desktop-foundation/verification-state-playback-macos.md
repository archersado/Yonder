# 四状态素材接线独立验证

2026-09-14预览反馈FAIL：执行爪子局部贴图动作不自然；用户明确全部状态保留呼吸/眨眼。返回Apply：独立眼区闭合层避免全身眨眼淡化，四态共用基础呼吸；执行短臂须肩部带动且身体底图补齐，不能挪开剪切层留下洞。演示仅视觉夹具，真实任务及其他五态来源门禁保持。

2026-09-14用户回归：v5双爪可见，但尾巴分层裁切痕迹与呼吸体验失败，返回Apply。斜折线剪切边界在skewY时产生相对位移；待命仅scaleY弱于既有起伏/歪头。修复沿既有尾摆要求，只分离尾尖且使切线与剪切零位移轴一致，恢复此前待命呼吸公式，不重新生成形象，不改变任务语义。

此次修复独立复验：尾尖分层x=26%与skewY零位移轴重合，尾根及双脚保持原图；ego-browser原交互回归与新增待命translateY/rotate/scale连续变化、裁切轴一致检查通过。离线构建及diff检查通过，预览正常退出再单实例启动。macOS原生60张窗口截图确认动态变化、双爪及透明边缘保持，九图接触表目测未见尾巴连接处张开，第7→8→9帧捕捉闭眼再睁开。证据state-assets-v4-20260914/tail-breath-v5/native-result.json、motion-check.json、native-contact.jpg。恢复此前呼吸公式不等同用户主观流畅度最终认可；Windows与完整Story门禁保持。

2026-09-14：关联DS-S1 RUN-ASSET-01～04与AD-DS-01，范围macOS研发。页面夹具状态切换/末帧保持/减少动态效果静态执行/未知回退/hover交互通过，证据apps/desktop/evidence/state-playback-20260914。阻断新PNG加载的旧图回退通过；构建、架构检查通过。

用户真实桌面反馈：新帧残留棋盘背景，且四帧硬切不如旧版丝滑。整体验证FAIL，返回Apply。禁用新序列并正常退出、封装、启动旧版表现，防止继续显示失败图片。页面测试不能证明背景精细提取或原生动画流畅，旧通过结论仅保留页面行为范围。

修复采用新一批纯色分离底素材，真实alpha与深浅背景边缘检查后才开放；序列改帧间连续过渡并保留微呼吸，不能凭四帧索引变化称丝滑通过。Windows按用户要求暂缓；Story不Done/Archive。

## v4回归修复复验

九种状态重新生成纯绿色分离底，保留原图；输出36张400×400 RGBA透明帧，边界检查与可见绿色残留0通过（assets/mascot/lifecycle-v4/alpha-check.json）。深浅背景人工检查未见棋盘格。v2已标为拒绝，不再被正式pet引用。

整图执行淡化出现爪部重影，拒绝；最终改为v4专注完整关键姿态+独立左右爪连续微转，身体保持稳定。修复多层遮罩的subtract/add顺序，消除左爪叠加过亮。页面夹具复验所有原hover/menu/拖动/状态打断/减少动态效果/未知行为通过，左右爪矩阵连续变化、身体固定、透明度权重之和为1。证据state-assets-v4-20260914/browser-result.json及browser-executing-local-fixed.png。页面操控只用明确夹具，不写正式任务。

macOS单实例原生小龙截图透明且完整，native-v4/native-result.json与image-check.json确认可见200×201窗口、透明像素、三张图像变化。最终宿主PID86869，正常托盘退出旧进程后启动，无第二小龙。

用户强调眨眼不得缺失：独立3–5秒调度保留，idle使用新闭眼帧短过渡，停靠双眼复用既有闭眼素材。最终宿主原生连续60张小龙窗口截图，23.79秒；第42→43→44张睁眼→闭眼→睁眼，暗蓝眼区像素约1100→289→1107，人工三联图确认双眼闭合后重新睁开。证据blink/blink-result.json、blink/open-closed-open.jpg；不是精确频率测量，采样可能漏掉130ms眨眼。页面独立闭眼帧与再次睁眼检查也通过（browser-blink.json）。减少动态效果与拖动仍优先停止装饰动画。

结论：背景回归与idle原生眨眼已复验通过；最终执行局部动画仅页面夹具通过，真实running/等待/暂停各自原生全链路与九态完整语义仍待验证，Windows暂缓。不能称全部动作完美或完整Story Done/Archive。

2026-09-14后续用户回归：idle只清晰可见一只手。检查原始idle-source.png与提取idle-00.png均如此，问题来自生成解剖姿态，不是尾巴遮罩。ASSET-01待命双短爪目测失败，返回素材Apply；只修订idle四帧，保留透明提取、闭眼帧、连续呼吸及尾摆，其他状态不改。

v5待命修订复验：保留v4原图，仅替换idle四张runtime PNG为v5双短爪，透明检查通过（4张400×400 RGBA、可见绿色残留0）。离线锁定构建、架构检查通过。单实例宿主PID90135重启后，原生60张窗口截图9.53秒，窗口200×201且当前桌面可见；人工三联图确认两个短前爪与双脚分开，透明边缘干净，第16→17→18帧捕捉闭眼再睁开，眼区像素1202→468→1205。证据idle-arms-v5-retry/native-result.json、blink-check.json、open-closed-open.jpg。首次截图因暂态窗口坐标异常失败，保留idle-arms-v5记录；重试可见坐标755,433通过，未据此修改定位代码。ASSET-01此次idle双爪回归通过，其他状态和完整Story未新增完成结论，Windows仍暂缓。

2026-09-15 v10 收口：正式宿主四态持续消费真实 `pet_task_state`，等待用户改用四张完整透明姿态往返抬爪，取消局部手臂拼接；录制道具仍仅存在于独立预览。ego-browser 捕获等待用户全部 `0/1/2/3` 姿态、呼吸和眨眼，状态打断与减少动态效果回归通过；macOS 应用离线构建、重新打包并保持单实例运行。证据见 `verification-waiting-recording-v10.md`。据此 RUN-ASSET-01～04 与本 macOS 播放子目标完成；Windows及九态真实事件接线继续保留，不关闭完整 DS-S1 Goal。
