# 独立 Verification Goal：等待分层修复与录制相机 v8

2026-09-14，关联DS-S1三份设计用户变更、生命周期规格及持续验收目标。

## 结论与范围

九态预览结构/时序采样PASS；用户满意与原生完整验收尚未证明，Goal继续，Story不Done/Archive。Windows按用户要求暂缓。录制相机仅在预览接线，不启动真实录制。

## 缺陷及修复证据

用户指出等待用户手臂裁切、等待外部响应鳞片脱离。原前臂椭圆边界截断前臂，扩大覆盖并柔化肩部衔接、轻举爪停顿；头部轮廓遗漏两小块侧鳞片，把对应区域并入头部层。scale-ownership.json验证(105,223)、(134,250)头部alpha大于200、身体alpha小于5。极值放大截图未见原先悬空鳞片与硬切口；并不据此宣称所有时刻的原生接缝通过。

录制首版相机被前臂挡住，返回实施并调整为前臂→相机→指尖渲染。镜头可见，相机与前臂共享连续举放矩阵，4.8秒一轮、每轮仅镜头附近一次低强度短闪光。生成原图camera-source.png含外部光晕，保留原图后按授权Pillow提取轮廓、输出透明camera.png，未直接使用带背景原图。

极值浅色背景发现尾部细线，改为普通alpha混合并在零位移轴处覆盖1源像素；最终浅色截图检查。

## 运行证据

同一ego-browser space21/p1，执行check-animation-preview.mjs，上方17秒下方11秒，检查九态呼吸/眨眼、零整图混合、独立头/爪/翼、录制相机举放/闪光、等待提示。最终目录：apps/desktop/evidence/state-assets-v4-20260914/wait-fixes-camera-v8-tail，result.json及深浅背景截图。动作极值与鳞片归属证据在wait-fixes-camera-v8-retry。此前失败与被挡相机截图保留在wait-fixes-camera-v8。

Node语法检查、git diff --check、Pillow透明画布自检通过。真实事件接线、系统减少动态效果的本轮原生证据、用户认可自然程度仍缺失，不能将预览PASS用于完整目标完成。

## 后续真实桌面待命证据

已核对正式库：仅4条cancelled，无运行中任务。重启唯一正式宿主到当前debug编译版本，PID31341，原生可见窗口200×200。check-state-assets-macos.swift连续截取60帧，耗时6.34秒，无任务变更。证据native-v8-idle/native-result.json及native-0..59.png。

眼区颜色计数第43→44→45帧为807→0→856；目测对应睁眼、闭眼、再睁眼，blink-analysis.json通过。这证明当前原生待命眨眼实际运行，不覆盖其他状态或系统减少动态效果。本轮原生截图角色完整、透明边界未见棋盘。录制/外部响应等五态仍为独立预览，完整目标继续。

## 2026-09-15 状态打断、减少动态效果与语义标记

首次独立打断检查等待短闪光超时：页面仍正常播放，轮询窗口遗漏300毫秒闪光。改用MutationObserver记录真实style变化；第二次因CDP媒体仿真缺少`media:''`未进入减少动态效果，保留两个failure.txt。修正后 interruptions-v8-retry/result.json PASS：退出录制立即撤下相机/闪光；减少动态效果保持静态相机且闪光为0；暂停隐藏独立展开翼片、保留收翼主体；进入idle撤下装饰。该证据是浏览器仿真，不替代真实系统设置。

语义辨识新增非交互标记：收到请求✓、等待外部响应连续省略点、等待用户?、暂停Ⅱ。semantic-cues-v8/result.json检查提示显示与省略点变化，九态呼吸/眨眼和零整图混合同时通过；top.png供人工检查。用户满意仍未确认，Goal保持Apply。
