# TM-S3 外部控制请求

关联 Story TM-S3 与 Accepted AD-TM-08。Architecture Impact：architecture-change（协议1.6、SQLite schema10及任务卡片接线）。用户或所属Agent可为running任务登记pause/cancel/takeover pending控制；响应只表示正在停止，执行器后续确认边界。定位与Recording不在本变更。
