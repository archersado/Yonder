# 设计

初始请求使用 `after_sequence=0, limit=20`。最后返回序号小于详情快照序号时显示“加载更多时间线”。点击后以最后实际显示序号继续请求 20 条；只接受严格递增事件，追加后依据同一快照序号决定是否继续显示按钮。

页面保存的只有当前选择轮次、已显示 DOM 和按钮提交态。空后续页或异常序号显示局部错误；切换任务、刷新或筛选后旧结果无效。事件文案继续复用现有状态、声明、observed 与 unknown 映射。
