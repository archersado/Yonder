# VI-S1 macOS 显式语音闭环 Verification Goal

日期：2026-09-18  
结论：PASS（已授权会话、真实采集与停止释放子范围）

## 目标

在正式Yonda桌宠进程内验证：只有用户显式点击后才开始采集；真实麦克风输入产生非空中文最终结果；最终文字无需确认并自动投递给开始时绑定的Agent；完成、手动停止和取消都释放本轮采集资源。证据不得保存PCM或转写正文。

## 结果

- 桌宠“语音输入”按钮打开同进程收音卡，状态进入“正在聆听”。
- 使用系统合成的公开测试短句经扬声器与真实麦克风形成统一声学样本；产生非空`zh-CN`最终结果，证据不记录正文。
- 首轮最终结果自动投递，界面显示“已交给智能体”，没有编辑或发送确认页。
- 首轮完成后立即再次开始成功，证明前一轮采集资源已经释放并可重新取得。
- 第二轮点击停止后生成最终结果；Agent会话断开时显示“当前没有可接收输入的 Agent”，没有伪报成功或自动重试。
- Esc取消并关闭收音卡，桌宠恢复非语音状态；未发现PCM落盘。
- 采集中执行应用级麦克风权限重置后，系统继续提供音频且没有撤权事件；该操作不能代替真实运行中撤权验证。
- 本机系统音频清单只有一个输入设备，无法构成真实设备切换样本。

结构化证据：`apps/desktop/evidence/voice-input-macos-20260918/result.json`。

## 未覆盖

本机权限在验证前已经授权；对adhoc预览包执行应用级麦克风和语音识别权限重置后，系统仍直接进入已授权采集，无法据此形成真实拒绝证据。采集中重置权限也没有产生撤权事件，且本机只有一个输入设备。本Goal不覆盖首次权限弹窗、真实运行中撤权和输入设备切换。Windows按用户决定暂缓。AD-VI-01继续保持Proposed，完整VI-S1不得Archive。
