# 普通桌面往返证据

2026-09-12，DS-S1，PID 66418，窗口 20605。normal-spaces.jsonl 保留首轮检查错误和最终完整通过记录；三张 PNG 只捕获小龙所在屏幕区域。

系统初始只有一个普通桌面。首轮添加桌面后，原“桌面”变为“桌面1”，新增为“桌面2”；基于旧标题的校验识别到两个新名称，因此停止。已通过原生列表单独确认真实新增为“桌面2”，续作才使用该名称完成测试和清理。

**diagnostic-recovery.swift 是这一次测试的恢复取证脚本，不是通用工具，不得对其他桌面布局直接复用固定“桌面2”清理条件。** 无用户文件被创建、移动或删除。最终普通桌面数量恢复为 1，原应用窗口 AXRaise 返回 0。
